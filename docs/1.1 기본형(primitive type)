논리형(boolean)
  true, false - 대소문자 구분함
  (ex) boolean checked = true;

문자형(char)
  하나의 문자를 유니코드(정수)값으로 저장
  (ex) char ch = 'A'; // A -> 65로 저장
  문자의 유니코드(UTF-8)를 알고싶으면 (int)ch 형태로 출력
  ** 문자는 숫자로 인코딩되어 컴퓨터에 저장되며, 이때 사용된 인코딩 코드와 맞는 디코딩방법으로
     디코딩해야만 정상적으로 출력된다.
     html 문서의 인코딩 코드표와 웹브라우저의 디코딩 코드표가 다르면 글자가 깨진다.

정수형(byte, short, int, long)
  JVM의 피연산자 스택(operand stack)이 피연산ㄴ자를 4byte 단위로 저장하기 때문에
  int를 사용하는것이 효율적.
  long타입의 범위를 벗어나는 값은, 실수형이나 BigInteger클래스를 사용

실수형(float, double)
  실수형 값을 저장할 때 float이 아닌 double타입의 변수를 사용하는 경우는
  저장하려는 '값의 범위'보다 '보다 높은 정밀도'가 필요하기때문
  float - 연산속도의 향상이나 메모리 절약해야 할 때
  double - 더 큰 값의 범위나 더 높은 정밀도가 필요할 때

형변환(casting)
  변수 또는 상수의 타입을 다른 타입으로 변환하는 것
    - 형변환 방법 : (타입)피연산자
      (ex) (int)dNum;


~을 코드화하다 : encode
~을 암호화하다 : encode
